import Utils from '../../utils.js'

const BASE_PATH = ''
const utils = new Utils(`${BASE_PATH}/input.txt`, `${BASE_PATH}/expected-result.txt`)

function solution(input) {
  return input
}

utils.validateSolution(solution)
